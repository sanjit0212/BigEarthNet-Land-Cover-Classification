# Python
venv/
__pycache__/
*.pyc

# Scala / SBT
target/
project/target/
project/project/target/
.bloop/
.metals/

# VS Code
.vscode/

# Data Files & Results
bigearthnet_subset/
metadata.parquet
*.log
BigEarthNet-S2.tar.zst
features/
results/predictions/
state/

# Keep documentation and small CSVs if desired, but explicitly ignore the massive raw data
# (results/*.json summaries, results/splits.parquet, and results/figures/ ARE tracked --
# only the large per-run raw prediction parquets under results/predictions/ are excluded)
